package com.ms.hogwarts.students.dtos;

public record OptionQuestionResponseDto(Long id,
                                        String key,
                                        String label,
                                        String meaning) {
}
